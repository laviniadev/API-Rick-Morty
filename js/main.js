// js/main.js
import { highlightActiveLink } from './dom.js'; // caminho correto, pois dom.js está direto em /js

document.addEventListener('DOMContentLoaded', () => {
  highlightActiveLink(); // ativa link do header
});
